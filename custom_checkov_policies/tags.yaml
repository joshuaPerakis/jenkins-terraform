metadata:
  id: CUS_AWS_1
  name: Check that resources have required tags
  category: CONVENTION
scope:
  provider: aws
definition:
  cond_type: attribute
  attribute: "tags_all.test"
  operator: exists
  resource_types:
    - aws_s3_bucket